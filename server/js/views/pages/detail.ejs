<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../components/head %>
</head>
<body>
    <main>
        <h1>
            <a href="/">Web App From Scratch</a>
        </h1>
        <section>
                <ul>
                    <li class="detail-image-wrapper">
                        <img 
                            class="head-image" 
                            srcset="https://cors-anywhere.herokuapp.com/<%= details.urls && details.urls.thumb %> 200w, https://cors-anywhere.herokuapp.com/<%= details.urls && details.urls.small %> 400w, https://cors-anywhere.herokuapp.com/<%= details.urls && details.urls.regular %> 1080w"
                            sizes="(max-width: 480px) 200px,
                                (min-width: 480px and max-width: 600px) 400px,
                                1080px"
                            src="https://cors-anywhere.herokuapp.com/<%= details.urls.regular %>"
                            style="background-color: <%= image.color %>;"
                            alt="image"
                        >
                    </li>
                    <li class="detail-image-wrapper">
                        <img 
                            class="user"
                            src="<%= details.user && details.user['profile_image'] && details.user['profile_image'].large %>" 
                            alt="user"
                        >
                    </li>
                    <li>
                         Username: <%= details.user.name %>
                    </li>
                    <li>
                         Width: <%= details.width %>
                    </li>
                    <li>
                         Height: <%= details.height %>
                    </li>
                    <li>
                         Likes: <%= details.likes %>
                    </li>
                </ul>
        </section>
    </main>
</body>
</html>